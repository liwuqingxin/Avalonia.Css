^Container {
    Background: var(control-back);
    Padding: 8;
    BorderBrush: var(control-border);
    BorderThickness: 1;
    CornerRadius: 5;
    Transitions:[
        var(stBackground);
    ]

    [[
        [UsePointerReaction=True]:pointerover{
            Background: var(control-back-hover);
        }
    ]]
}