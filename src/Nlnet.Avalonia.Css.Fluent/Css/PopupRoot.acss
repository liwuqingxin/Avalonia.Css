
PopupRoot {
    [[
        PopupRoot /template/ Border#PopupBorder{
            BoxShadow: var(popup-shadow);
            CornerRadius: 6;
            BorderThickness: 1;
            Margin: 10,10,10,10;
            Background: White;
            BorderBrush:var(control-border);
            ClipToBounds:False;
        }
    ]]
}