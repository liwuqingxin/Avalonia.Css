^Window {
    Background: var(window-back);
    TransparencyBackgroundFallback: Transparent;
    Foreground: var(fore);
    TopLevel.SystemBarColor: var(control-back);
    ScrollViewer.IsScrollChainingEnabled: True;
    PopupStyles.Style: Fade;
}
